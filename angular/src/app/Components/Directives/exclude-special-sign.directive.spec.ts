import { ExcludeSpecialSignDirective } from './exclude-special-sign.directive';

describe('ExcludeSpecialSignDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcludeSpecialSignDirective();
    expect(directive).toBeTruthy();
  });
});
